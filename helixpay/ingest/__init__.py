"""HelixPay ingestion: embeddings, extraction, resolution, contradictions, pipeline.

Owned by Agent 2 (SP_003). Agent 1 owns the disjoint ``loaders/`` subpackage.
All DB access goes through ``helixpay.contracts.Repository``; cross-module types
come from ``helixpay.contracts`` and are never redefined here.
"""
