"""SP_015 — the one-document-per-type proving harness.

A curated *one doc per content archetype* corpus + a per-doc proving bar (golden facts +
loss-ledger completeness + a $0 persisted-embedding check) used to prove the pipeline
extracts every document type without silent loss, as cheaply as possible — before any full
44-doc run. See ``workspace/sprints/SP_015_one_per_type_proving.md``.
"""
